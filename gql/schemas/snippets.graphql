type Snippet {
  id: ID
  title: String
  content: String
  userId: ID
  slug: String
  snippetType: Int
  favoritesCount: Int
  favorited: Boolean!
  createdAt: Time
  updatedAt: Time
  lockVersion: Int!
}

type MsSnippet {
  id: ID
  title: String
  content: String
  userId: ID
  slug: String
  snippetType: Int
  favoritesCount: Int
  favorited: Boolean!
  createdAt: Time
  updatedAt: Time
  lockVersion: Int!
}

input SnippetsQuery {
  titleCont: String
}

type SnippetsCollection {
  collection: [MsSnippet!]
  metadata: Metadata!
}

type SnippetFavorite {
  favorited: Boolean!
}

type SnippetModification {
  snippet: Snippet!
}

input SnippetInput {
  title: String
  content: String
  snippetType: Int
  passkey: String
  lockVersion: Int
}
