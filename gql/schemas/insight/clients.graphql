type Client {
  id: ID
  name: String
  createdAt: Time
  updatedAt: Time
}

input ClientsQuery {
  nameCont: String
}

type ClientCollection {
  collection: [Client!]
  metadata: Metadata!
}

input ClientCreateInput {
  name: String
}

type ClientModification {
  client: Client
}